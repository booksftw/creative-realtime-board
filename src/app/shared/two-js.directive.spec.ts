import { TwoJsDirective } from './two-js.directive';

describe('TwoJsDirective', () => {
  it('should create an instance', () => {
    const directive = new TwoJsDirective();
    expect(directive).toBeTruthy();
  });
});

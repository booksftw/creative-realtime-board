import { GarbageDirective } from './garbage.directive';

describe('GarbageDirective', () => {
  it('should create an instance', () => {
    const directive = new GarbageDirective();
    expect(directive).toBeTruthy();
  });
});

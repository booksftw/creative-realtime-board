import { AtramentPaintDirective } from './atrament-paint.directive';

describe('AtramentPaintDirective', () => {
  it('should create an instance', () => {
    const directive = new AtramentPaintDirective();
    expect(directive).toBeTruthy();
  });
});

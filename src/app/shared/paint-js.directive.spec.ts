import { PaintJsDirective } from './paint-js.directive';

describe('PaintJsDirective', () => {
  it('should create an instance', () => {
    const directive = new PaintJsDirective();
    expect(directive).toBeTruthy();
  });
});

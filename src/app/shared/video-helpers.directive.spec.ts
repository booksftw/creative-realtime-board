import { VideoHelpersDirective } from './video-helpers.directive';

describe('VideoHelpersDirective', () => {
  it('should create an instance', () => {
    const directive = new VideoHelpersDirective();
    expect(directive).toBeTruthy();
  });
});

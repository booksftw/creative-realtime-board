import { DrawWithPenDirective } from './draw-with-pen.directive';

describe('DrawWithPenDirective', () => {
  it('should create an instance', () => {
    const directive = new DrawWithPenDirective();
    expect(directive).toBeTruthy();
  });
});
